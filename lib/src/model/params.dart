enum TuxAppearance {
  filled,
  outline,
  ghost,
}
enum TuxWidgetState {
  normal,
  hover,
  active,
  focus,
  disabled,
}
enum TuxWidgetSize {
  giant,
  large,
  medium,
  small,
  tiny,
}
enum TuxStatus {
  primary,
  success,
  info,
  warning,
  danger,
}

enum TuxImageAvatar { circle, square, rounded }

enum TuxShape {
  circle,
  square,
  rounded,
  roundedTop,
  roundedBottom,
  roundedLeft,
  roundedRight
}

enum TuxCardStatus {
  none,
  strip,
  header,
}

enum TuxHorizontalPositioning {
  left,
  right,
  none,
}

enum TuxVerticalPositioning {
  top,
  bottom,
}
